---
phase: 07-adapter-verification
plan: 03
type: execute
wave: 3
depends_on: [07-02]
files_modified:
  - public/data/scores-by-tohop.json
  - public/data/universities.json
  - public/data/tohop.json
autonomous: true
requirements: [PIPE-03, INFRA-02]

must_haves:
  truths:
    - "generate-static produces scores-by-tohop.json with real cutoff data from verified adapters"
    - "Updated static JSON files are committed to public/data/"
    - "scores-by-tohop.json has more than 0 entries (real data, not empty)"
  artifacts:
    - path: "public/data/scores-by-tohop.json"
      provides: "Static fallback cutoff score data for CDN"
      min_lines: 5
    - path: "public/data/universities.json"
      provides: "Static fallback university list for CDN"
      min_lines: 5
    - path: "public/data/tohop.json"
      provides: "Static fallback tohop code list for CDN"
      min_lines: 5
  key_links:
    - from: "scripts/generate-static-json.ts"
      to: "public/data/"
      via: "writeFileSync to public/data/*.json"
      pattern: "writeFileSync"
    - from: "public/data/scores-by-tohop.json"
      to: "app/"
      via: "Vercel CDN serves static JSON as fallback"
      pattern: "scores-by-tohop"
---

<objective>
Run generate-static-json.ts to populate public/data/ with real cutoff score data from verified adapters, then commit the updated JSON files.

Purpose: Fulfill CONTEXT.md locked decision "Run generate-static after verified adapters scrape successfully" and "Commit updated JSON to public/data/". This satisfies Phase 7 Success Criterion 3: "scores-by-tohop.json contains real cutoff data after generate-static re-run".
Output: Updated public/data/scores-by-tohop.json, universities.json, and tohop.json with real data.
</objective>

<execution_context>
@/Users/thangduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thangduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-adapter-verification/07-CONTEXT.md
@.planning/phases/07-adapter-verification/07-02-SUMMARY.md

<interfaces>
From scripts/generate-static-json.ts:
```typescript
// Reads from Supabase DB (requires DATABASE_URL env var)
// Writes three JSON files to public/data/:
//   - universities.json (list of universities)
//   - scores-by-tohop.json (cutoff scores grouped by tohop)
//   - tohop.json (tohop code list)
// Run: npx tsx scripts/generate-static-json.ts
// Or: npm run generate-static
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run generate-static and verify output contains real data</name>
  <files>public/data/scores-by-tohop.json, public/data/universities.json, public/data/tohop.json</files>
  <read_first>
    scripts/generate-static-json.ts
    public/data/scores-by-tohop.json
    .planning/phases/07-adapter-verification/07-02-SUMMARY.md
  </read_first>
  <action>
1. Run the static JSON generator:
```bash
npx tsx scripts/generate-static-json.ts
```
This requires DATABASE_URL env var (loaded via @next/env from .env.local).

2. Verify output:
   - Check that `public/data/scores-by-tohop.json` is non-empty and contains actual score entries (not just `[]` or `{}`)
   - Check that `public/data/universities.json` contains university entries
   - Check that `public/data/tohop.json` contains tohop code entries
   - If scores-by-tohop.json is empty, it means the verified adapters have not yet been run against the real DB. In that case, run the scraper first for verified adapters:
     ```bash
     npx tsx lib/scraper/run.ts
     ```
     Then re-run generate-static.

3. After confirming non-empty output, the updated JSON files should be committed (the executor should stage and commit `public/data/*.json` with message noting the data refresh).

**IMPORTANT:** If DATABASE_URL is not available or DB has no scraped data, document this in the SUMMARY as a blocker. The generate-static script will exit with an error if DATABASE_URL is missing.
  </action>
  <verify>
    <automated>node -e "const d = require('./public/data/scores-by-tohop.json'); const u = require('./public/data/universities.json'); console.log('scores entries:', Array.isArray(d) ? d.length : Object.keys(d).length); console.log('universities:', Array.isArray(u) ? u.length : Object.keys(u).length); if ((Array.isArray(d) ? d.length : Object.keys(d).length) === 0) { process.exit(1); }" && echo "PASS"</automated>
  </verify>
  <done>public/data/scores-by-tohop.json contains real cutoff data entries (non-empty). public/data/universities.json and tohop.json are also populated. All three files committed to git.</done>
</task>

</tasks>

<verification>
- `public/data/scores-by-tohop.json` is non-empty and contains score entries
- `public/data/universities.json` contains university entries
- `public/data/tohop.json` contains tohop code entries
- Files are committed to git
</verification>

<success_criteria>
1. generate-static-json.ts runs successfully against the database
2. scores-by-tohop.json contains real cutoff data (not empty)
3. All three public/data/ JSON files are committed
</success_criteria>

<output>
After completion, create `.planning/phases/07-adapter-verification/07-03-SUMMARY.md`
</output>
