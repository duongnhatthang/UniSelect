---
phase: 06-tech-debt-cleanup
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/utils/tohop-subjects.ts
  - components/ScoreForm.tsx
  - app/api/scores/route.ts
  - app/api/years/route.ts
  - lib/api/scores.ts
  - tests/api/scores.test.ts
  - tests/api/years.test.ts
  - package.json
  - .planning/phases/04-scraper-expansion/04-01-SUMMARY.md
  - .planning/phases/04-scraper-expansion/04-02-SUMMARY.md
autonomous: true
requirements: [SCOR-01, PIPE-04]

must_haves:
  truths:
    - "ScoreForm tohop dropdown shows Vietnamese label text (e.g. 'A00 — Toan - Ly - Hoa') not just raw code and subjects"
    - "No /api/scores or /api/years endpoints exist — requests to those URLs 404"
    - "check-staleness script is runnable via npm run check-staleness"
    - "Phase 4 SUMMARY files list PIPE-04 in requirements_completed"
  artifacts:
    - path: "lib/utils/tohop-subjects.ts"
      provides: "TohopCode type with label_vi field"
      contains: "label_vi"
    - path: "components/ScoreForm.tsx"
      provides: "Dropdown rendering with label_vi"
      contains: "label_vi"
    - path: "package.json"
      provides: "check-staleness script entry"
      contains: "check-staleness"
  key_links:
    - from: "components/ScoreForm.tsx"
      to: "lib/utils/tohop-subjects.ts"
      via: "TohopCode type import"
      pattern: "TohopCode"
    - from: "components/ScoreForm.tsx"
      to: "/api/tohop"
      via: "fetch for dropdown data including label_vi"
      pattern: "label_vi"
---

<objective>
Clean up four tech debt items from the v1.0 milestone audit: fix ScoreForm dropdown labels to show Vietnamese text via TohopCode.label_vi, remove orphaned /api/scores and /api/years routes plus their test files, register check-staleness in package.json scripts, and add PIPE-04 to Phase 4 SUMMARY frontmatter.

Purpose: Eliminate code quality gaps before the v1.0 milestone is closed.
Output: Cleaner codebase with correct UI labels, no dead endpoints, and complete documentation.
</objective>

<execution_context>
@/Users/thangduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thangduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@lib/utils/tohop-subjects.ts
@components/ScoreForm.tsx
@lib/db/schema.ts
@app/api/tohop/route.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix TohopCode type and ScoreForm dropdown labels</name>
  <files>lib/utils/tohop-subjects.ts, components/ScoreForm.tsx</files>
  <read_first>
    - lib/utils/tohop-subjects.ts (current TohopCode type — missing label_vi)
    - components/ScoreForm.tsx (line 137-139 — current dropdown rendering)
    - lib/db/schema.ts (tohopCodes table — confirms label_vi column exists)
    - app/api/tohop/route.ts (confirms API returns label_vi from DB)
    - components/UniversitySearch.tsx (reference implementation — already uses label_vi correctly on line 94)
  </read_first>
  <action>
1. In `lib/utils/tohop-subjects.ts`, add `label_vi` to the `TohopCode` interface:
```typescript
export interface TohopCode {
  code: string;
  subjects: string[];
  label_vi: string | null;
}
```

2. In `components/ScoreForm.tsx`, update the dropdown option rendering at line 137-139. Change:
```typescript
{tc.code} ({tc.subjects.join(', ')})
```
to:
```typescript
{tc.code}{tc.label_vi ? ` — ${tc.label_vi}` : ` (${tc.subjects.join(', ')})`}
```
This matches the pattern already used in `components/UniversitySearch.tsx` line 94. When `label_vi` is available (e.g. "Toan - Ly - Hoa"), show it. When null, fall back to subjects list.

3. Also in `components/ScoreForm.tsx`, update the `useState` type annotation on line 22 and the fetch response type on line 32. The fetch already returns `label_vi` from the API (the DB column exists and the /api/tohop route selects all columns), but the old `TohopCode` type was silently discarding it. After updating the interface, the type flows correctly through the existing code.
  </action>
  <verify>
    <automated>grep -n "label_vi" lib/utils/tohop-subjects.ts && grep -n "label_vi" components/ScoreForm.tsx</automated>
  </verify>
  <acceptance_criteria>
    - lib/utils/tohop-subjects.ts contains `label_vi: string | null`
    - components/ScoreForm.tsx contains `tc.label_vi`
    - components/ScoreForm.tsx does NOT contain `tc.subjects.join` as the primary display (only as fallback)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>ScoreForm dropdown shows Vietnamese labels when label_vi is available, falls back to subject list when null. TohopCode type includes label_vi field.</done>
</task>

<task type="auto">
  <name>Task 2: Remove orphaned routes, register script, fix SUMMARY docs</name>
  <files>app/api/scores/route.ts, app/api/years/route.ts, lib/api/scores.ts, tests/api/scores.test.ts, tests/api/years.test.ts, package.json, .planning/phases/04-scraper-expansion/04-01-SUMMARY.md, .planning/phases/04-scraper-expansion/04-02-SUMMARY.md</files>
  <read_first>
    - app/api/scores/route.ts (confirm orphaned — no frontend references)
    - app/api/years/route.ts (confirm orphaned — no frontend references)
    - lib/api/scores.ts (only imported by app/api/scores/route.ts)
    - tests/api/scores.test.ts (test for orphaned route)
    - tests/api/years.test.ts (test for orphaned route)
    - package.json (current scripts section)
    - .planning/phases/04-scraper-expansion/04-01-SUMMARY.md (frontmatter — missing PIPE-04)
    - .planning/phases/04-scraper-expansion/04-02-SUMMARY.md (frontmatter — missing PIPE-04)
  </read_first>
  <action>
1. **Delete orphaned files** (5 files):
   - `app/api/scores/route.ts` — delete file and remove the `app/api/scores/` directory
   - `app/api/years/route.ts` — delete file and remove the `app/api/years/` directory
   - `lib/api/scores.ts` — delete file (only consumer was the scores route)
   - `tests/api/scores.test.ts` — delete file
   - `tests/api/years.test.ts` — delete file

2. **Register check-staleness in package.json** — add to the `"scripts"` section:
```json
"check-staleness": "tsx scripts/check-staleness.ts"
```
Place it after the `"load-test"` entry. The full scripts block should be:
```json
"scripts": {
  "dev": "next dev --turbopack",
  "build": "next build --webpack",
  "start": "next start",
  "lint": "eslint",
  "generate-static": "tsx scripts/generate-static-json.ts",
  "load-test": "tsx scripts/load-test.ts",
  "check-staleness": "tsx scripts/check-staleness.ts"
}
```

3. **Fix Phase 4 SUMMARY frontmatter** — in both `04-01-SUMMARY.md` and `04-02-SUMMARY.md`, add a `requirements_completed` field to the frontmatter YAML block. It does not currently exist, so add it after the `metrics` block:
```yaml
requirements_completed: [PIPE-04]
```
  </action>
  <verify>
    <automated>test ! -f app/api/scores/route.ts && test ! -f app/api/years/route.ts && test ! -f lib/api/scores.ts && test ! -f tests/api/scores.test.ts && test ! -f tests/api/years.test.ts && grep "check-staleness" package.json && grep "requirements_completed.*PIPE-04" .planning/phases/04-scraper-expansion/04-01-SUMMARY.md && grep "requirements_completed.*PIPE-04" .planning/phases/04-scraper-expansion/04-02-SUMMARY.md && echo "ALL CHECKS PASSED"</automated>
  </verify>
  <acceptance_criteria>
    - app/api/scores/route.ts does NOT exist
    - app/api/years/route.ts does NOT exist
    - lib/api/scores.ts does NOT exist
    - tests/api/scores.test.ts does NOT exist
    - tests/api/years.test.ts does NOT exist
    - package.json contains `"check-staleness": "tsx scripts/check-staleness.ts"`
    - 04-01-SUMMARY.md frontmatter contains `requirements_completed: [PIPE-04]`
    - 04-02-SUMMARY.md frontmatter contains `requirements_completed: [PIPE-04]`
    - `npx tsc --noEmit` exits 0 (no broken imports)
  </acceptance_criteria>
  <done>Orphaned /api/scores and /api/years routes deleted with their tests and helper. check-staleness registered in package.json. Phase 4 SUMMARYs include PIPE-04.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` — TypeScript compiles cleanly (no broken imports from deleted files, no type errors from TohopCode change)
2. `grep "label_vi" lib/utils/tohop-subjects.ts` — confirms label_vi in type
3. `grep "label_vi" components/ScoreForm.tsx` — confirms label_vi used in dropdown
4. `test ! -d app/api/scores && test ! -d app/api/years` — orphaned routes gone
5. `grep "check-staleness" package.json` — script registered
6. `grep "PIPE-04" .planning/phases/04-scraper-expansion/04-01-SUMMARY.md` — docs fixed
</verification>

<success_criteria>
- ScoreForm tohop dropdown renders Vietnamese labels via label_vi (falling back to subjects when null)
- No /api/scores or /api/years routes exist in the codebase
- `npm run check-staleness` is a valid script command
- Phase 4 SUMMARY files document PIPE-04 requirement completion
- TypeScript compiles without errors
</success_criteria>

<output>
After completion, create `.planning/phases/06-tech-debt-cleanup/06-01-SUMMARY.md`
</output>
