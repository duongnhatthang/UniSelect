---
phase: 12-testing-ci
plan: "02"
subsystem: infra
tags: [github-actions, ci, gitignore, serwist, next.js]

# Dependency graph
requires:
  - phase: 12-testing-ci
    plan: "01"
    provides: "npm test script with vitest run"
provides:
  - PR-triggered GitHub Actions CI workflow running npm test + npm run build
  - Dead src/ scaffold removed from git tracking
  - public/sw.js untracked and listed in .gitignore
affects:
  - all future pull requests (CI will block merges on failure)
  - any phase adding new build artifacts (follow serwist pattern for .gitignore)

# Tech tracking
tech-stack:
  added: [github-actions]
  patterns:
    - "GitHub Actions workflow follows same structure as scrape-low.yml: checkout@v4, setup-node@v4 with node-version: 20 and cache: npm"
    - "Secrets with fallback placeholders pattern: ${{ secrets.VAR || 'fallback' }} allows fork PRs to build without access to repo secrets"
    - "Build artifacts generated by framework tooling (Serwist, Next.js static generation) go in .gitignore and are untracked with git rm --cached"

key-files:
  created:
    - .github/workflows/ci.yml
  modified:
    - .gitignore
  deleted:
    - src/app/favicon.ico
    - src/app/globals.css
    - src/app/layout.tsx
    - src/app/page.tsx
    - public/sw.js (untracked from git, kept on disk)

key-decisions:
  - "Build step uses secrets with placeholder fallbacks so fork PRs without repo secrets can still run the workflow without failing on env var access"
  - "CI triggers only on pull_request (not push) per TEST-02 requirement — push events are not gated"
  - "public/sw.js added under a new serwist comment section in .gitignore for clarity"

patterns-established:
  - "CI workflow: ubuntu-latest, Node 20, npm cache, sequential npm ci -> npm test -> npm run build"

requirements-completed: [TEST-02, TEST-03, TEST-04]

# Metrics
duration: 1min
completed: 2026-03-19
---

# Phase 12 Plan 02: CI Workflow and Repository Cleanup Summary

**PR-triggered GitHub Actions CI (npm test + build), dead src/ scaffold removed, public/sw.js untracked from git index**

## Performance

- **Duration:** ~1 min
- **Started:** 2026-03-19T07:14:35Z
- **Completed:** 2026-03-19T07:15:34Z
- **Tasks:** 2
- **Files modified:** 6 (1 created, 1 modified, 4 deleted, 1 untracked)

## Accomplishments

- Created `.github/workflows/ci.yml` that triggers on pull_request, installs deps, runs the full 513-test vitest suite, and then runs the Next.js build
- Removed 4 dead Next.js scaffold files from `src/app/` that were git-tracked duplicates of files already in `app/`
- Added `public/sw.js` to `.gitignore` and removed it from git index with `git rm --cached` — file remains on disk as a Serwist build artifact

## Task Commits

Each task was committed atomically:

1. **Task 1: Create GitHub Actions CI workflow** - `60b1549` (feat)
2. **Task 2: Remove dead src/ directory and untrack public/sw.js** - `c8f17d2` (chore)

## Files Created/Modified

- `.github/workflows/ci.yml` - PR-triggered CI: checkout, Node 20, npm ci, npm test, npm run build with Supabase env placeholder fallbacks
- `.gitignore` - Added `public/sw.js` under new `# serwist build artifacts` section
- `src/app/favicon.ico`, `src/app/globals.css`, `src/app/layout.tsx`, `src/app/page.tsx` - Deleted (unused Next.js scaffold duplicates)
- `public/sw.js` - Untracked from git (kept on disk)

## Decisions Made

- Build step provides placeholder fallback values for Supabase env vars so fork PRs without repo secrets can run the workflow without immediately failing on env var absence
- CI triggers only on `pull_request` (not `push`) as specified by TEST-02 — push events are not gated
- New `# serwist build artifacts` comment section added to `.gitignore` for clarity about why this specific file is excluded

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered

None.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- CI is live and will trigger on the next PR opened against this repo
- 513 tests pass (29 test files) confirming test infrastructure from Plan 01 is stable
- Repository is clean: no dead scaffold files tracked, no build artifacts committed

---
*Phase: 12-testing-ci*
*Completed: 2026-03-19*
