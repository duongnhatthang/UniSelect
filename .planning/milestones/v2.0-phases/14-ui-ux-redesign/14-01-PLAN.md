---
phase: 14-ui-ux-redesign
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - app/layout.tsx
  - components/DarkModeToggle.tsx
  - messages/vi.json
  - messages/en.json
autonomous: true
requirements: [UI-01, UI-02, UI-08]

must_haves:
  truths:
    - "Tailwind v4 @theme block defines semantic color tokens (primary, surface, on-surface, border)"
    - "Be Vietnam Pro font is applied via --font-sans token mapped to --font-be-vietnam"
    - "Dark mode toggle switches between light and dark themes without page reload"
    - "Dark mode persists across page reloads via localStorage (no white flash)"
    - "Body element uses semantic token classes (bg-surface, text-on-surface) not hardcoded colors"
  artifacts:
    - path: "app/globals.css"
      provides: "@theme tokens, @custom-variant dark, :root/.dark CSS variables"
      contains: "@custom-variant dark"
    - path: "app/layout.tsx"
      provides: "ThemeProvider wrapping, suppressHydrationWarning"
      contains: "ThemeProvider"
    - path: "components/DarkModeToggle.tsx"
      provides: "Dark mode toggle button using next-themes useTheme"
      contains: "useTheme"
  key_links:
    - from: "app/globals.css"
      to: "app/layout.tsx"
      via: "@theme inline --font-sans maps to --font-be-vietnam variable set by Next.js font"
      pattern: "--font-sans.*--font-be-vietnam"
    - from: "components/DarkModeToggle.tsx"
      to: "app/layout.tsx"
      via: "ThemeProvider provides theme context consumed by useTheme"
      pattern: "ThemeProvider"
---

<objective>
Establish the design token system, fix the font application, and implement dark mode.

Purpose: All subsequent UI work depends on the semantic token system. The font fix resolves a known bug where Be Vietnam Pro is loaded but not applied. Dark mode requires the token system to be in place first.

Output: globals.css with @theme tokens and dark mode variables, layout.tsx with ThemeProvider, DarkModeToggle component, updated i18n messages.
</objective>

<execution_context>
@/Users/thangduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thangduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ui-ux-redesign/14-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From app/layout.tsx (current — will be modified):
```tsx
import { Be_Vietnam_Pro } from 'next/font/google';
const font = Be_Vietnam_Pro({ variable: '--font-be-vietnam' });
// Currently: <html className={font.variable}> <body className="font-sans antialiased bg-white text-gray-900">
// Needs: suppressHydrationWarning on <html>, ThemeProvider wrapping, semantic classes on body
```

From app/globals.css (current — single line):
```css
@import "tailwindcss";
```

From messages/vi.json, messages/en.json:
- Need new key: common.toggleDarkMode
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Design tokens, font fix, and dark mode CSS</name>
  <files>app/globals.css, messages/vi.json, messages/en.json</files>
  <read_first>app/globals.css, app/layout.tsx, .planning/phases/14-ui-ux-redesign/14-RESEARCH.md</read_first>
  <action>
Replace app/globals.css with the full design token system:

1. Keep `@import "tailwindcss";` at top
2. Add `@custom-variant dark (&:where(.dark, .dark *));` for Tailwind v4 dark mode
3. Add `@theme inline { --font-sans: var(--font-be-vietnam); }` to fix the font-sans token (UI-02). Use `@theme inline` (not bare `@theme`) so the CSS variable resolves at runtime per research.
4. Add `@layer base` with `:root` and `.dark` blocks defining semantic color tokens:
   - --color-primary: oklch(0.55 0.18 240) (light) / oklch(0.68 0.18 240) (dark)
   - --color-primary-hover: oklch(0.48 0.18 240) / oklch(0.62 0.18 240)
   - --color-surface: oklch(1.0 0 0) / oklch(0.13 0 0)
   - --color-surface-subtle: oklch(0.97 0 0) / oklch(0.18 0 0)
   - --color-on-surface: oklch(0.11 0 0) / oklch(0.95 0 0)
   - --color-on-surface-muted: oklch(0.45 0 0) / oklch(0.65 0 0)
   - --color-border: oklch(0.92 0 0) / oklch(0.27 0 0)
5. Add second `@theme inline` block to register semantic colors with Tailwind:
   - --color-primary, --color-primary-hover, --color-surface, --color-surface-subtle, --color-on-surface, --color-on-surface-muted, --color-border

6. Add i18n keys to both messages files:
   - vi.json: `"toggleDarkMode": "Chuyển chế độ tối"` inside "common"
   - en.json: `"toggleDarkMode": "Toggle dark mode"` inside "common"

Do NOT use bare `@theme` for font-sans — it must be `@theme inline` per research (Pitfall 3).
  </action>
  <verify>
    <automated>cd /Users/thangduong/Desktop/UniSelect && grep -q "@custom-variant dark" app/globals.css && grep -q "font-sans" app/globals.css && grep -q "color-primary" app/globals.css && grep -q "toggleDarkMode" messages/vi.json && grep -q "toggleDarkMode" messages/en.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - app/globals.css contains `@custom-variant dark`
    - app/globals.css contains `@theme inline` with `--font-sans: var(--font-be-vietnam)`
    - app/globals.css contains `:root` block with `--color-primary`
    - app/globals.css contains `.dark` block with `--color-primary`
    - messages/vi.json contains `toggleDarkMode`
    - messages/en.json contains `toggleDarkMode`
  </acceptance_criteria>
  <done>globals.css has design tokens with light/dark variants, font-sans token maps to Be Vietnam Pro, i18n messages updated</done>
</task>

<task type="auto">
  <name>Task 2: ThemeProvider integration and DarkModeToggle component</name>
  <files>app/layout.tsx, components/DarkModeToggle.tsx</files>
  <read_first>app/layout.tsx, app/page.tsx, components/LocaleToggle.tsx, .planning/phases/14-ui-ux-redesign/14-RESEARCH.md</read_first>
  <action>
1. Create components/DarkModeToggle.tsx:
   - 'use client' directive
   - Import useTheme from 'next-themes', useTranslations from 'next-intl'
   - Render a button that toggles between 'light' and 'dark' themes
   - Use sun/moon icons (inline SVG — no icon library dependency): sun icon when dark, moon icon when light
   - Add `aria-label={t('toggleDarkMode')}` for accessibility
   - Style with semantic tokens: `p-2 rounded-lg border border-border hover:bg-surface-subtle transition-colors`
   - Handle mounted state: render null until mounted (useEffect + useState) to avoid hydration mismatch on the icon

2. Modify app/layout.tsx:
   - Add `import { ThemeProvider } from 'next-themes';`
   - Add `suppressHydrationWarning` to the `<html>` element
   - Change body classes from `bg-white text-gray-900` to `bg-surface text-on-surface`
   - Wrap children in ThemeProvider INSIDE NuqsAdapter (ThemeProvider wraps NextIntlClientProvider):
     ```
     <NuqsAdapter>
       <ThemeProvider attribute="class" defaultTheme="system" enableSystem disableTransitionOnChange>
         <NextIntlClientProvider messages={messages}>
           <OfflineBanner />
           {children}
         </NextIntlClientProvider>
       </ThemeProvider>
     </NuqsAdapter>
     ```

3. Modify app/page.tsx:
   - Import DarkModeToggle
   - Add DarkModeToggle next to LocaleToggle in the header (flex gap-2 container)
   - Update header hardcoded colors: `bg-white` -> `bg-surface`, `border-gray-200` -> `border-border`, `text-gray-900` -> `text-on-surface`
   - Update main bg: `bg-gray-50` -> `bg-surface-subtle`

CRITICAL: `suppressHydrationWarning` on `<html>` is REQUIRED — next-themes modifies the class attribute client-side. Without it, React throws hydration mismatch warnings.
  </action>
  <verify>
    <automated>cd /Users/thangduong/Desktop/UniSelect && grep -q "ThemeProvider" app/layout.tsx && grep -q "suppressHydrationWarning" app/layout.tsx && grep -q "bg-surface" app/layout.tsx && grep -q "useTheme" components/DarkModeToggle.tsx && grep -q "DarkModeToggle" app/page.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - app/layout.tsx contains `ThemeProvider`
    - app/layout.tsx contains `suppressHydrationWarning`
    - app/layout.tsx body has `bg-surface text-on-surface` (not bg-white text-gray-900)
    - components/DarkModeToggle.tsx contains `useTheme`
    - app/page.tsx imports and renders DarkModeToggle
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>ThemeProvider wraps the app, dark mode toggle is in the header, body uses semantic token classes, build passes</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `grep -q "@custom-variant dark" app/globals.css` confirms dark variant registered
- `grep -q "ThemeProvider" app/layout.tsx` confirms provider wrapping
- `grep -q "suppressHydrationWarning" app/layout.tsx` confirms no-flash setup
- `grep -q "font-sans.*font-be-vietnam" app/globals.css` confirms font token mapping
</verification>

<success_criteria>
- Design token system is established in globals.css with @theme inline blocks
- Be Vietnam Pro font maps to --font-sans (DevTools will show "Be Vietnam Pro" as computed font-family)
- Dark mode toggle in header switches themes; preference persists via localStorage
- No white flash on dark-mode page reload (suppressHydrationWarning + next-themes script injection)
- Body uses semantic classes (bg-surface, text-on-surface) not hardcoded colors
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-ux-redesign/14-01-SUMMARY.md`
</output>
