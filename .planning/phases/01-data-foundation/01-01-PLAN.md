---
phase: 01-data-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - tsconfig.json
  - next.config.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - lib/db/schema.ts
  - lib/db/index.ts
  - drizzle.config.ts
  - drizzle/migrations/0001_init.sql
  - vitest.config.mts
  - .env.example
  - .gitignore
autonomous: true
requirements: [PIPE-01, INFRA-01]

must_haves:
  truths:
    - "Next.js project builds successfully with npx next build"
    - "Drizzle schema defines all 5 tables: universities, majors, tohop_codes, cutoff_scores, scrape_runs"
    - "Migration file contains INSERT statements seeding 78+ universities from uni_list_examples.md"
    - "DB connection uses Supabase pooler port 6543 with prepare: false"
    - "Vitest runs and reports 0 tests (no test files yet)"
  artifacts:
    - path: "lib/db/schema.ts"
      provides: "Drizzle schema with 5 tables"
      exports: ["universities", "majors", "tohopCodes", "cutoffScores", "scrapeRuns"]
    - path: "lib/db/index.ts"
      provides: "Database connection via postgres.js + drizzle"
      exports: ["db"]
    - path: "drizzle/migrations/0001_init.sql"
      provides: "Schema DDL + university seed data"
      contains: "INSERT INTO universities"
    - path: "drizzle.config.ts"
      provides: "Drizzle Kit configuration"
    - path: "vitest.config.mts"
      provides: "Vitest test framework configuration"
    - path: ".env.example"
      provides: "Required environment variable documentation"
      contains: "DATABASE_URL"
  key_links:
    - from: "lib/db/index.ts"
      to: "lib/db/schema.ts"
      via: "import * as schema"
      pattern: "import.*schema"
    - from: "drizzle.config.ts"
      to: "lib/db/schema.ts"
      via: "schema path reference"
      pattern: "schema.*lib/db/schema"
---

<objective>
Initialize the Next.js 15 project, define the complete Drizzle ORM schema for all 5 database tables, create the migration file with university seed data, and configure the test framework.

Purpose: Every subsequent plan depends on these files existing — the schema types are imported by the scraper framework, the DB connection is used by the runner, and Vitest is needed for test tasks.
Output: A buildable Next.js project with Drizzle schema, migration SQL, DB connection module, and Vitest configuration.
</objective>

<execution_context>
@/Users/thangduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thangduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-data-foundation/01-CONTEXT.md
@.planning/phases/01-data-foundation/01-RESEARCH.md
@uni_list_examples.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js 15 project and install all Phase 1 dependencies</name>
  <files>package.json, tsconfig.json, next.config.ts, src/app/layout.tsx, src/app/page.tsx, .env.example, .gitignore</files>
  <read_first>
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Standard Stack section for exact versions and install commands)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (locked decisions)
  </read_first>
  <action>
    1. Run `npx create-next-app@latest . --typescript --tailwind --app --src-dir --no-import-alias` in the project root (the project has no src/ yet, this is greenfield).
       - If create-next-app fails because directory is non-empty (due to .planning/, etc.), use `--yes` flag or create in a temp dir and move files.
    2. Install ORM and driver:
       `npm install drizzle-orm postgres`
       `npm install -D drizzle-kit`
    3. Install Supabase client:
       `npm install @supabase/supabase-js`
    4. Install scraping dependencies:
       `npm install cheerio iconv-lite chardet`
    5. Install test framework:
       `npm install -D vitest @vitejs/plugin-react vite-tsconfig-paths`
    6. Create `.env.example` with these exact lines:
       ```
       # Supabase Pooler URL (port 6543, NOT direct 5432)
       DATABASE_URL=postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres
       # Supabase service role key (for scraper writes)
       SUPABASE_SERVICE_ROLE_KEY=eyJ...
       ```
    7. Ensure `.gitignore` includes `.env`, `.env.local`, `node_modules/`, `.next/`.
    8. Update `src/app/page.tsx` to render a minimal placeholder: `<h1>UniSelect</h1><p>Coming soon</p>`.
    9. Verify the project builds: `npx next build`.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "drizzle-orm" in dependencies
    - package.json contains "postgres" in dependencies
    - package.json contains "cheerio" in dependencies
    - package.json contains "iconv-lite" in dependencies
    - package.json contains "chardet" in dependencies
    - package.json contains "vitest" in devDependencies
    - package.json contains "drizzle-kit" in devDependencies
    - .env.example contains "DATABASE_URL="
    - .env.example contains "SUPABASE_SERVICE_ROLE_KEY="
    - .gitignore contains ".env"
    - npx next build exits with code 0
  </acceptance_criteria>
  <done>Next.js 15 project builds successfully with all Phase 1 dependencies installed. .env.example documents required environment variables.</done>
</task>

<task type="auto">
  <name>Task 2: Define Drizzle schema, DB connection, migration with seed data, and Vitest config</name>
  <files>lib/db/schema.ts, lib/db/index.ts, drizzle.config.ts, drizzle/migrations/0001_init.sql, vitest.config.mts</files>
  <read_first>
    - uni_list_examples.md (authoritative seed list — 78 rows of tab-separated: ministry code, name_vi, website URL)
    - .planning/phases/01-data-foundation/01-RESEARCH.md (Pattern 1: Drizzle Schema, Pattern 5: Supabase Connection, Vitest Config, drizzle.config.ts examples)
    - .planning/phases/01-data-foundation/01-CONTEXT.md (locked decisions on columns, seed mechanism, scrape_runs fields)
    - .planning/research/ARCHITECTURE.md (schema sketch, index requirements)
    - package.json (verify dependencies are installed)
  </read_first>
  <action>
    1. Create `lib/db/schema.ts` with these exact table definitions using drizzle-orm/pg-core:

       **universities table:**
       - id: text('id').primaryKey() — Ministry code e.g. "BKA"
       - name_vi: text('name_vi').notNull()
       - name_en: text('name_en') — nullable, for Phase 3 i18n
       - website_url: text('website_url')
       - created_at: timestamptz('created_at').defaultNow()

       **majors table:**
       - id: text('id').primaryKey() — 7-digit ma nganh e.g. "7480201"
       - name_vi: text('name_vi').notNull()
       - created_at: timestamptz('created_at').defaultNow()

       **tohop_codes table:**
       - code: text('code').primaryKey() — e.g. "A00", "D01"
       - subjects: text('subjects').array().notNull()
       - label_vi: text('label_vi')

       **cutoff_scores table:**
       - id: bigserial('id', { mode: 'number' }).primaryKey()
       - university_id: text('university_id').notNull().references(() => universities.id)
       - major_id: text('major_id').notNull().references(() => majors.id)
       - tohop_code: text('tohop_code').notNull().references(() => tohopCodes.code)
       - year: smallint('year').notNull()
       - score: numeric('score', { precision: 5, scale: 2 }) — nullable (NULL if not published)
       - admission_method: text('admission_method').notNull().default('THPT')
       - source_url: text('source_url')
       - scraped_at: timestamptz('scraped_at').defaultNow()
       - UNIQUE constraint on (university_id, major_id, tohop_code, year, admission_method) using `unique()` helper

       **scrape_runs table:**
       - id: bigserial('id', { mode: 'number' }).primaryKey()
       - run_at: timestamptz('run_at').defaultNow()
       - university_id: text('university_id').references(() => universities.id)
       - status: text('status') — "ok" | "error" | "flagged"
       - rows_written: integer('rows_written')
       - rows_rejected: integer('rows_rejected')
       - error_log: text('error_log') — JSON array of rejection details (stored as text)
       - github_run_id: text('github_run_id')

       Export all tables as named exports: `universities`, `majors`, `tohopCodes`, `cutoffScores`, `scrapeRuns`.

    2. Create `lib/db/index.ts`:
       ```typescript
       import { drizzle } from 'drizzle-orm/postgres-js';
       import postgres from 'postgres';
       import * as schema from './schema';

       // Use POOLER URL (port 6543) not direct DB URL (port 5432)
       // Set prepare: false for transaction pool mode (Supabase Supavisor)
       const client = postgres(process.env.DATABASE_URL!, { prepare: false });
       export const db = drizzle({ client, schema });
       ```

    3. Create `drizzle.config.ts`:
       ```typescript
       import { defineConfig } from 'drizzle-kit';

       export default defineConfig({
         schema: './lib/db/schema.ts',
         out: './drizzle/migrations',
         dialect: 'postgresql',
         dbCredentials: {
           url: process.env.DATABASE_URL!,
         },
       });
       ```

    4. Create `drizzle/migrations/0001_init.sql` — a CUSTOM migration file containing:
       - Full DDL for all 5 tables (write the CREATE TABLE statements manually matching the Drizzle schema exactly — do NOT run drizzle-kit generate, since there's no live DB yet)
       - CREATE INDEX statements:
         - `CREATE INDEX idx_cutoff_uni_year ON cutoff_scores (university_id, year);`
         - `CREATE INDEX idx_cutoff_tohop_year ON cutoff_scores (tohop_code, year);`
         - `CREATE INDEX idx_cutoff_score_year_tohop ON cutoff_scores (score, year, tohop_code);`
       - INSERT statements for ALL 78 universities from `uni_list_examples.md`, tab-separated format:
         `INSERT INTO universities (id, name_vi, website_url) VALUES ('QH', 'DAI HOC QUOC GIA HA NOI', 'https://www.vnu.edu.vn/home/'), ... ON CONFLICT (id) DO NOTHING;`
         - IMPORTANT: Preserve exact Vietnamese diacritics from uni_list_examples.md. Strip leading "- " prefix from sub-university names (e.g., "- Truong Dai hoc Luat" becomes "Truong Dai hoc Luat").
         - Use single SQL INSERT with multiple VALUES rows, not one INSERT per row.

    5. Create `vitest.config.mts`:
       ```typescript
       import { defineConfig } from 'vitest/config';
       import react from '@vitejs/plugin-react';
       import tsconfigPaths from 'vite-tsconfig-paths';

       export default defineConfig({
         plugins: [tsconfigPaths(), react()],
         test: {
           environment: 'node',
           include: ['**/*.test.ts', '**/*.test.tsx'],
         },
       });
       ```

    6. Verify `npx next build` still passes after adding these files.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5 && echo "---" && grep -c "INSERT INTO universities" drizzle/migrations/0001_init.sql && echo "---" && grep -c "export const" lib/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - lib/db/schema.ts contains "export const universities = pgTable("
    - lib/db/schema.ts contains "export const majors = pgTable("
    - lib/db/schema.ts contains "export const tohopCodes = pgTable("
    - lib/db/schema.ts contains "export const cutoffScores = pgTable("
    - lib/db/schema.ts contains "export const scrapeRuns = pgTable("
    - lib/db/schema.ts contains "name_en" (nullable column for Phase 3)
    - lib/db/schema.ts contains "unique()" constraint on cutoff_scores
    - lib/db/schema.ts contains "rows_rejected" on scrape_runs
    - lib/db/schema.ts contains "error_log" on scrape_runs
    - lib/db/index.ts contains "prepare: false"
    - lib/db/index.ts contains "port 6543" or "pooler" in a comment
    - drizzle.config.ts contains "schema: './lib/db/schema.ts'"
    - drizzle/migrations/0001_init.sql contains "CREATE TABLE universities"
    - drizzle/migrations/0001_init.sql contains "CREATE TABLE cutoff_scores"
    - drizzle/migrations/0001_init.sql contains "CREATE TABLE scrape_runs"
    - drizzle/migrations/0001_init.sql contains "CREATE INDEX idx_cutoff_uni_year"
    - drizzle/migrations/0001_init.sql contains "CREATE INDEX idx_cutoff_score_year_tohop"
    - drizzle/migrations/0001_init.sql contains "INSERT INTO universities" with at least 78 value rows
    - drizzle/migrations/0001_init.sql contains "'BKA'" (ministry code present)
    - drizzle/migrations/0001_init.sql contains "'NTH'" (ministry code present)
    - drizzle/migrations/0001_init.sql contains "ON CONFLICT (id) DO NOTHING"
    - vitest.config.mts contains "environment: 'node'"
    - vitest.config.mts contains "tsconfigPaths()"
    - npx next build exits with code 0
  </acceptance_criteria>
  <done>Drizzle schema defines all 5 tables with correct types, constraints, and indexes. Migration file seeds 78+ universities. DB connection uses Supabase pooler with prepare:false. Vitest is configured. Project still builds.</done>
</task>

</tasks>

<verification>
- `npx next build` exits 0
- `grep -c "pgTable" lib/db/schema.ts` returns 5
- `grep -c "INSERT INTO universities" drizzle/migrations/0001_init.sql` returns at least 1
- `grep "prepare: false" lib/db/index.ts` finds the setting
- `npx vitest run 2>&1 | head -5` runs without config errors
</verification>

<success_criteria>
1. Next.js 15 project builds with zero errors
2. All 5 Drizzle tables defined with correct column types and constraints
3. Migration SQL contains DDL + indexes + 78+ university seed rows
4. DB connection configured for Supabase pooler (port 6543, prepare: false)
5. Vitest configured and runnable
6. .env.example documents DATABASE_URL and SUPABASE_SERVICE_ROLE_KEY
</success_criteria>

<output>
After completion, create `.planning/phases/01-data-foundation/01-01-SUMMARY.md`
</output>
