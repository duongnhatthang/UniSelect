---
phase: 14-ui-ux-redesign
plan: 04
type: execute
wave: 3
depends_on: [14-02, 14-03]
files_modified:
  - components/ScoreForm.tsx
  - components/NguyenVongList.tsx
  - components/ResultsList.tsx
  - components/OfflineBanner.tsx
  - app/page.tsx
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08, UI-09]

must_haves:
  truths:
    - "All hardcoded gray/white/blue colors in components are replaced with semantic token classes"
    - "The full UI renders correctly in both light and dark mode"
    - "Build passes and all tests pass"
  artifacts:
    - path: "components/ScoreForm.tsx"
      provides: "Semantic token classes throughout"
      contains: "bg-surface"
    - path: "components/NguyenVongList.tsx"
      provides: "Semantic token classes throughout"
      contains: "text-on-surface"
  key_links:
    - from: "app/globals.css"
      to: "all components"
      via: "Semantic token classes resolve to CSS custom properties"
      pattern: "bg-surface|text-on-surface|border-border"
---

<objective>
Migrate remaining hardcoded colors to semantic tokens and verify the complete UI visually.

Purpose: Plans 01-03 established the token system and built features, but ScoreForm and other components still use hardcoded colors (text-gray-900, bg-white, border-gray-300, etc.). This plan completes the migration and adds a visual checkpoint.

Output: All components using semantic tokens, visual verification of complete UI in light and dark mode.
</objective>

<execution_context>
@/Users/thangduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thangduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-ui-ux-redesign/14-01-SUMMARY.md
@.planning/phases/14-ui-ux-redesign/14-02-SUMMARY.md
@.planning/phases/14-ui-ux-redesign/14-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate all remaining hardcoded colors to semantic tokens</name>
  <files>components/ScoreForm.tsx, components/NguyenVongList.tsx, components/OfflineBanner.tsx, app/page.tsx</files>
  <read_first>components/ScoreForm.tsx, components/OfflineBanner.tsx, app/page.tsx</read_first>
  <action>
Search all component files for hardcoded color classes and replace with semantic tokens. The mapping is:

| Hardcoded | Semantic Token |
|-----------|---------------|
| bg-white | bg-surface |
| bg-gray-50 | bg-surface-subtle |
| bg-gray-100 | bg-surface-subtle |
| text-gray-900 | text-on-surface |
| text-gray-700 | text-on-surface |
| text-gray-600 | text-on-surface-muted |
| text-gray-500 | text-on-surface-muted |
| text-gray-400 | text-on-surface-muted |
| border-gray-200 | border-border |
| border-gray-300 | border-border |
| bg-blue-600 | bg-primary |
| bg-blue-500 | bg-primary |
| hover:bg-blue-700 | hover:bg-primary-hover |
| text-blue-600 | text-primary |
| text-blue-500 | text-primary |
| border-blue-500 | border-primary |
| focus:ring-blue-500 | focus:ring-primary |

EXCEPTIONS — Keep tier-specific colors as-is (they are semantic within their context):
- text-amber-600, text-green-600 (trend colors from Phase 11 decision)
- text-blue-500/bg-blue-50, text-green-500/bg-green-50, text-amber-500/bg-amber-50 (TierBadge tier colors)
- text-red-600, bg-red-50, border-red-200 (error states)

Apply to these files specifically:
1. components/ScoreForm.tsx — mode tabs, form inputs, labels, submit button, detailed total box
2. components/OfflineBanner.tsx — banner styling
3. app/page.tsx — any remaining hardcoded colors not caught in Plan 01

Run `grep -rn "text-gray-\|bg-gray-\|bg-white\|border-gray-\|text-blue-\|bg-blue-\|border-blue-" components/ app/page.tsx` before and after to verify completeness. Some gray classes in loading skeletons and error states can stay if they serve a specific visual purpose.
  </action>
  <verify>
    <automated>cd /Users/thangduong/Desktop/UniSelect && REMAINING=$(grep -rn "bg-white\b\|text-gray-900\|text-gray-700\|border-gray-200\|border-gray-300\|bg-blue-600\|hover:bg-blue-700" components/ScoreForm.tsx components/OfflineBanner.tsx app/page.tsx 2>/dev/null | wc -l); echo "Remaining hardcoded: $REMAINING"; npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/ScoreForm.tsx contains `bg-surface` (not bg-white for cards)
    - components/ScoreForm.tsx contains `bg-primary` (not bg-blue-600 for submit)
    - components/ScoreForm.tsx contains `text-on-surface` (not text-gray-900 for labels)
    - Fewer than 3 hardcoded color references remain in ScoreForm.tsx (exceptions only)
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>All main components use semantic tokens; hardcoded colors only remain for tier/trend/error-specific coloring</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of complete UI redesign</name>
  <files>none</files>
  <action>
Present the completed UI to the user for visual verification. Start the dev server with `npm run dev` if not already running. Guide the user through the 8-step verification checklist covering all 9 UI requirements.
  </action>
  <what-built>
Complete Phase 14 UI/UX redesign:
- Design token system with semantic colors (UI-01)
- Be Vietnam Pro font fix (UI-02)
- Error boundaries with error.tsx and not-found.tsx (UI-03)
- Editable nguyen vong list with add/remove/reorder (UI-04)
- Tier grouping headers with 5+5+5 strategy (UI-05)
- Onboarding banner for first-time visitors (UI-06)
- Tier badge tooltips showing score margins (UI-07)
- Dark mode toggle with persistence (UI-08)
- Pre-submission empty state with guidance text (UI-09)
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000

2. Font check (UI-02): Open DevTools > Computed > font-family. Should show "Be Vietnam Pro" as first entry.

3. Onboarding (UI-06): Open in incognito/private window. A banner should appear explaining what UniSelect does. Click dismiss — reload the page, banner should not reappear.

4. Empty state (UI-09): Before submitting a score, the results area should show guidance text (not "No matching results").

5. Submit a score (e.g., tohop A00, score 22.0):
   - Results should display with "+" buttons (UI-04)
   - Click "+" on 3-4 results to add them to the nguyen vong list
   - The nguyen vong list should show tier grouping headers (UI-05)
   - Use up/down arrows to reorder items (UI-04)
   - Remove an item with X (UI-04)
   - Hover/tap a tier badge — tooltip should show score margin (UI-07)

6. Dark mode (UI-08): Click the moon/sun toggle in the header. The entire page should switch to dark theme. Reload — should stay in dark mode (no white flash).

7. Error boundary (UI-03): Navigate to http://localhost:3000/nonexistent — should see a 404 page with Vietnamese text and a home link.

8. Token migration: In dark mode, check that all text is readable, no white/gray backgrounds leak through.
  </how-to-verify>
  <verify>
    <automated>cd /Users/thangduong/Desktop/UniSelect && npm test 2>&1 | tail -10 && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - User confirms font is Be Vietnam Pro in DevTools
    - User confirms dark mode toggle works and persists
    - User confirms onboarding banner shows and dismisses
    - User confirms editable nguyen vong list works
    - User confirms 404 page renders correctly
  </acceptance_criteria>
  <done>User approves the complete UI/UX redesign visually</done>
  <resume-signal>Type "approved" or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- `npm test` passes all tests
- Visual verification confirms all 9 UI requirements working
</verification>

<success_criteria>
- All components use semantic token classes
- Light and dark mode both render correctly
- Full UI/UX redesign is visually verified by user
- Build and tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-ux-redesign/14-04-SUMMARY.md`
</output>
