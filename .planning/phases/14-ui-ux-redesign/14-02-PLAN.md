---
phase: 14-ui-ux-redesign
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - app/error.tsx
  - app/not-found.tsx
  - components/OnboardingBanner.tsx
  - components/ResultsList.tsx
  - app/page.tsx
  - messages/vi.json
  - messages/en.json
autonomous: true
requirements: [UI-03, UI-06, UI-09]

must_haves:
  truths:
    - "Navigating to a non-existent route shows a Vietnamese/English 404 page with a home link"
    - "An uncaught error in a route segment shows the error boundary with retry button"
    - "First-time visitors see a dismissible onboarding banner before submitting their score"
    - "After dismissing the banner, it does not reappear on reload"
    - "Before submitting any score, the results area shows guidance text instead of 'No matching results'"
  artifacts:
    - path: "app/error.tsx"
      provides: "Client Component error boundary with retry and home link"
      contains: "use client"
    - path: "app/not-found.tsx"
      provides: "404 page with Vietnamese/English text and home link"
      contains: "NotFoundPage"
    - path: "components/OnboardingBanner.tsx"
      provides: "Dismissible banner using localStorage"
      contains: "onboarding-dismissed"
    - path: "components/ResultsList.tsx"
      provides: "Pre-submission empty state with guidance text"
      contains: "emptyStateBeforeSubmission"
  key_links:
    - from: "app/error.tsx"
      to: "messages/vi.json"
      via: "useTranslations('ErrorPage')"
      pattern: "ErrorPage"
    - from: "app/not-found.tsx"
      to: "messages/vi.json"
      via: "useTranslations('NotFoundPage')"
      pattern: "NotFoundPage"
    - from: "components/OnboardingBanner.tsx"
      to: "app/page.tsx"
      via: "Imported and rendered above ScoreForm"
      pattern: "OnboardingBanner"
---

<objective>
Add error boundaries, onboarding banner, and pre-submission empty state.

Purpose: Error boundaries catch runtime errors gracefully (currently crashes show Next.js default error page). The onboarding banner helps first-time users understand what UniSelect does. The empty state provides guidance before score submission instead of a confusing "No results" message.

Output: error.tsx and not-found.tsx error boundaries, OnboardingBanner component, updated ResultsList with pre-submission empty state, updated i18n messages.
</objective>

<execution_context>
@/Users/thangduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thangduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-ui-ux-redesign/14-RESEARCH.md
@.planning/phases/14-ui-ux-redesign/14-01-SUMMARY.md

<interfaces>
<!-- Key interfaces the executor needs -->

From components/ResultsList.tsx (current):
```tsx
interface ResultsListProps {
  results: RecommendResult[];
  loading: boolean;
  userScore: number;
}
// Currently: results.length === 0 returns "No matching results"
// Needs: distinguish between "no results after search" vs "hasn't searched yet"
```

From app/page.tsx:
```tsx
// OnboardingBanner will be added above the ScoreForm section
// page.tsx imports ScoreForm and renders it in <div className="max-w-2xl mx-auto py-6">
```

From messages/vi.json, messages/en.json:
- Need new namespaces: ErrorPage, NotFoundPage, OnboardingBanner
- Need new key: common.emptyStateBeforeSubmission
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Error boundaries and i18n messages</name>
  <files>app/error.tsx, app/not-found.tsx, messages/vi.json, messages/en.json</files>
  <read_first>messages/vi.json, messages/en.json, .planning/phases/14-ui-ux-redesign/14-RESEARCH.md</read_first>
  <action>
1. Add i18n message namespaces to BOTH vi.json and en.json:

For vi.json, add these top-level keys alongside "common":
```json
"ErrorPage": {
  "title": "Da co loi xay ra",
  "description": "Ung dung gap su co. Vui long thu lai hoac quay ve trang chu.",
  "retry": "Thu lai",
  "home": "Ve trang chu"
},
"NotFoundPage": {
  "title": "Khong tim thay trang",
  "description": "Trang ban dang tim khong ton tai hoac da bi di chuyen.",
  "backHome": "Ve trang chu"
},
"OnboardingBanner": {
  "message": "Nhap diem thi THPT va to hop cua ban de xem danh sach truong phu hop",
  "dismiss": "Da hieu"
}
```

For en.json, add:
```json
"ErrorPage": {
  "title": "Something went wrong",
  "description": "The application encountered an error. Please try again or return home.",
  "retry": "Try again",
  "home": "Go home"
},
"NotFoundPage": {
  "title": "Page not found",
  "description": "The page you are looking for does not exist or has been moved.",
  "backHome": "Go home"
},
"OnboardingBanner": {
  "message": "Enter your exam score and subject combination to see matching universities",
  "dismiss": "Got it"
}
```

Also add to common namespace in both files:
- vi.json: `"emptyStateBeforeSubmission": "Nhap diem thi o phia tren de xem danh sach truong dai hoc phu hop"`
- en.json: `"emptyStateBeforeSubmission": "Enter your score above to see matching universities"`

Use proper Vietnamese diacritics in the actual implementation (the research CONTEXT.md shows the exact Vietnamese text with diacritics).

2. Create app/error.tsx:
   - 'use client' directive (REQUIRED for error boundaries)
   - Import useTranslations from 'next-intl', Link from 'next/link'
   - Props: `{ error: Error & { digest?: string }; reset: () => void }`
   - Full-page centered layout using semantic tokens: `min-h-screen flex flex-col items-center justify-center gap-4 px-4 bg-surface`
   - Show title (text-xl font-bold text-on-surface), description (text-on-surface-muted text-sm)
   - Two buttons in a flex row: retry (calls reset()) styled as primary, home link styled as secondary
   - Retry button: `bg-primary text-white px-4 py-2 rounded-lg hover:bg-primary-hover`
   - Home link: `text-primary underline`

3. Create app/not-found.tsx:
   - Import useTranslations from 'next-intl', Link from 'next/link'
   - Full-page centered layout: `min-h-screen flex flex-col items-center justify-center gap-4 p-8 bg-surface`
   - Large "404" text (text-6xl font-bold text-on-surface-muted)
   - Title, description, and home link with semantic token classes
   - Home link: `text-primary underline`
  </action>
  <verify>
    <automated>cd /Users/thangduong/Desktop/UniSelect && grep -q "use client" app/error.tsx && grep -q "ErrorPage" app/error.tsx && grep -q "reset" app/error.tsx && grep -q "NotFoundPage" app/not-found.tsx && grep -q "404" app/not-found.tsx && grep -q "ErrorPage" messages/vi.json && grep -q "NotFoundPage" messages/en.json && grep -q "OnboardingBanner" messages/vi.json && grep -q "emptyStateBeforeSubmission" messages/vi.json && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - app/error.tsx contains `'use client'`
    - app/error.tsx contains `useTranslations('ErrorPage')`
    - app/error.tsx contains `reset` (retry function call)
    - app/not-found.tsx contains `useTranslations('NotFoundPage')`
    - app/not-found.tsx contains `404`
    - messages/vi.json contains `ErrorPage` namespace
    - messages/en.json contains `NotFoundPage` namespace
    - messages/vi.json contains `OnboardingBanner` namespace
    - messages/vi.json contains `emptyStateBeforeSubmission` key
  </acceptance_criteria>
  <done>Error boundary catches route errors with retry + home link, 404 page shows Vietnamese/English text, all i18n keys added for this plan and the onboarding banner</done>
</task>

<task type="auto">
  <name>Task 2: Onboarding banner and pre-submission empty state</name>
  <files>components/OnboardingBanner.tsx, components/ResultsList.tsx, app/page.tsx</files>
  <read_first>components/ResultsList.tsx, app/page.tsx, components/ScoreForm.tsx, messages/vi.json</read_first>
  <action>
1. Create components/OnboardingBanner.tsx:
   - 'use client' directive
   - Import useState, useEffect from 'react', useTranslations from 'next-intl'
   - Use useEffect pattern for localStorage access (not lazy useState initializer) per research recommendation:
     ```
     const [dismissed, setDismissed] = useState(true); // default hidden on SSR
     const [mounted, setMounted] = useState(false);
     useEffect(() => {
       setMounted(true);
       setDismissed(localStorage.getItem('onboarding-dismissed') === '1');
     }, []);
     ```
   - If !mounted or dismissed, return null
   - Render a banner with: info icon (inline SVG), message text from t('OnboardingBanner.message'), dismiss button with t('OnboardingBanner.dismiss')
   - On dismiss: `localStorage.setItem('onboarding-dismissed', '1'); setDismissed(true);`
   - Style: `bg-primary/10 border border-primary/20 rounded-lg p-4 flex items-start gap-3` in the max-w-2xl container
   - Dismiss button: `text-sm font-medium text-primary hover:underline whitespace-nowrap`

2. Modify components/ResultsList.tsx:
   - Add a new prop `hasSubmitted: boolean` to ResultsListProps
   - Before the existing `results.length === 0` check, add a pre-submission guard:
     ```
     if (!hasSubmitted && results.length === 0) {
       return (
         <div className="text-center py-8">
           <p className="text-sm text-on-surface-muted">{t('emptyStateBeforeSubmission')}</p>
         </div>
       );
     }
     ```
   - The existing `results.length === 0` block (after submission) stays as-is but update its text color: `text-gray-500` -> `text-on-surface-muted`
   - Also update hardcoded colors in the results cards: `bg-white` -> `bg-surface`, `text-gray-900` -> `text-on-surface`, `text-gray-600` -> `text-on-surface-muted`, `text-gray-400` -> `text-on-surface-muted`, `text-gray-500` -> `text-on-surface-muted`
   - Update loading skeleton: `bg-gray-50` -> `bg-surface-subtle`

3. Modify components/ScoreForm.tsx (where ResultsList is rendered):
   - Add a `hasSubmitted` state: `const [hasSubmitted, setHasSubmitted] = useState(false);`
   - Set `setHasSubmitted(true)` inside `fetchRecommendations` before the fetch call
   - Pass `hasSubmitted` to `<ResultsList results={results} loading={loading} userScore={activeScore ?? 0} hasSubmitted={hasSubmitted} />`

4. Modify app/page.tsx:
   - Import OnboardingBanner
   - Add `<OnboardingBanner />` inside the main content area, before the ScoreForm section:
     ```
     <div className="max-w-2xl mx-auto px-4 pt-4">
       <OnboardingBanner />
     </div>
     ```
   - Update remaining hardcoded colors in page.tsx: `text-gray-900` -> `text-on-surface`
  </action>
  <verify>
    <automated>cd /Users/thangduong/Desktop/UniSelect && grep -q "onboarding-dismissed" components/OnboardingBanner.tsx && grep -q "OnboardingBanner" app/page.tsx && grep -q "hasSubmitted" components/ResultsList.tsx && grep -q "emptyStateBeforeSubmission" components/ResultsList.tsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - components/OnboardingBanner.tsx contains `onboarding-dismissed` localStorage key
    - components/OnboardingBanner.tsx contains `'use client'`
    - app/page.tsx imports and renders `OnboardingBanner`
    - components/ResultsList.tsx accepts `hasSubmitted` prop
    - components/ResultsList.tsx contains `emptyStateBeforeSubmission` translation key
    - `npx next build` succeeds
  </acceptance_criteria>
  <done>Onboarding banner shows for first-time visitors and is dismissible; pre-submission empty state shows guidance text instead of "No matching results"; build passes</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `grep -q "use client" app/error.tsx` confirms error boundary is client component
- `grep -q "reset" app/error.tsx` confirms retry capability
- `grep -q "onboarding-dismissed" components/OnboardingBanner.tsx` confirms localStorage persistence
- `grep -q "emptyStateBeforeSubmission" components/ResultsList.tsx` confirms pre-submission guard
</verification>

<success_criteria>
- Navigating to /nonexistent shows 404 page with Vietnamese text and home link
- An uncaught error shows the error boundary with retry button and home link
- First-time visitors see an onboarding banner; dismissing it persists across reloads
- Before submitting a score, the results area shows guidance text (not "No matching results")
- All new components use semantic token classes (no hardcoded colors)
</success_criteria>

<output>
After completion, create `.planning/phases/14-ui-ux-redesign/14-02-SUMMARY.md`
</output>
