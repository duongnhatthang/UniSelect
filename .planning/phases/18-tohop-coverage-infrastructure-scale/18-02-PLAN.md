---
phase: 18-tohop-coverage-infrastructure-scale
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/scraper/registry.ts
  - lib/scraper/run.ts
  - .github/workflows/scrape-low.yml
  - .github/workflows/scrape-peak.yml
  - tests/scraper/run.test.ts
autonomous: true
requirements: [INFR-04, INFR-05]

must_haves:
  truths:
    - "SHARD_TYPE=cheerio filters registry to only cheerio adapters before sharding"
    - "SHARD_TYPE=playwright filters registry to only playwright adapters"
    - "SHARD_TYPE=paddleocr filters registry to only paddleocr adapters"
    - "SHARD_TYPE=all or unset includes all adapter types (backward compatible)"
    - "Workflow matrix has 24 jobs: 20 cheerio + 2 playwright + 2 paddleocr"
    - "Playwright setup steps only run on playwright shards"
    - "PaddleOCR setup steps only run on paddleocr shards"
    - "Cheerio shards skip both Playwright install and PaddleOCR warmup"
  artifacts:
    - path: "lib/scraper/registry.ts"
      provides: "ResolvedEntry with adapterType field"
      contains: "adapterType"
    - path: "lib/scraper/run.ts"
      provides: "SHARD_TYPE env var filtering before shard distribution"
      contains: "SHARD_TYPE"
    - path: ".github/workflows/scrape-low.yml"
      provides: "Type-aware matrix with 24 jobs and conditional setup steps"
      contains: "shard_type"
    - path: ".github/workflows/scrape-peak.yml"
      provides: "Same type-aware matrix as scrape-low.yml"
      contains: "shard_type"
    - path: "tests/scraper/run.test.ts"
      provides: "Unit tests for SHARD_TYPE filtering"
      contains: "SHARD_TYPE"
  key_links:
    - from: "lib/scraper/run.ts"
      to: "lib/scraper/registry.ts"
      via: "loadRegistry returns ResolvedEntry[] with adapterType"
      pattern: "adapterType"
    - from: ".github/workflows/scrape-low.yml"
      to: "lib/scraper/run.ts"
      via: "SHARD_TYPE env var passed from matrix.shard_type"
      pattern: "SHARD_TYPE.*matrix.shard_type"
---

<objective>
Scale the scrape workflow from 6 mixed shards to 24 type-isolated shards (20 cheerio + 2 playwright + 2 paddleocr), so cheerio shards skip expensive Playwright/OCR setup and no shard exceeds timeout limits at 400+ universities.

Purpose: INFR-04 requires shard count scaling for 400+ universities. INFR-05 requires Playwright/OCR isolation so cheerio shards never wait for browser launch or OCR model download.
Output: Extended registry.ts and run.ts with type filtering, updated workflow matrices, unit tests.
</objective>

<execution_context>
@/Users/thangduong/.claude/get-shit-done/workflows/execute-plan.md
@/Users/thangduong/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-tohop-coverage-infrastructure-scale/18-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. -->

From lib/scraper/registry.ts:
```typescript
interface RegistryEntry {
  id: string;
  adapter: string;
  website_url: string;
  scrape_url: string | null;
  adapter_type?: 'cheerio' | 'playwright' | 'paddleocr' | 'skip' | 'pending';
  note?: string;
  factory_config?: Omit<CheerioAdapterConfig, 'id'>;
}

interface ResolvedEntry {
  id: string;
  adapter: ScraperAdapter;
  url: string;
  // adapterType: string;  <-- TO ADD
}
```

From lib/scraper/run.ts (shard logic):
```typescript
const shardIndex = parseInt(process.env.SHARD_INDEX ?? '0', 10);
const shardTotal = parseInt(process.env.SHARD_TOTAL ?? '1', 10);
const shard = registry.filter((_, i) => i % shardTotal === shardIndex);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add adapterType to ResolvedEntry and SHARD_TYPE filtering to run.ts</name>
  <files>lib/scraper/registry.ts, lib/scraper/run.ts, tests/scraper/run.test.ts</files>
  <read_first>
    - lib/scraper/registry.ts (current ResolvedEntry interface and loadRegistry function)
    - lib/scraper/run.ts (current shard logic)
    - .planning/phases/18-tohop-coverage-infrastructure-scale/18-RESEARCH.md (Pattern 2: SHARD_TYPE Filter)
  </read_first>
  <behavior>
    - Test: SHARD_TYPE=cheerio filters out playwright and paddleocr entries
    - Test: SHARD_TYPE=playwright includes only playwright entries
    - Test: SHARD_TYPE=paddleocr includes only paddleocr entries
    - Test: SHARD_TYPE=all (or unset) includes all entries
    - Test: Shard modulo distribution applies AFTER type filtering
  </behavior>
  <action>
    1. In `lib/scraper/registry.ts`:
       - Add `adapterType: string` to `ResolvedEntry` interface
       - In the `loadRegistry` for loop, when pushing to `resolved`, add:
         `adapterType: entry.adapter_type ?? 'cheerio'`
       - The full push becomes: `resolved.push({ id: entry.id, adapter, url: entry.scrape_url, adapterType: entry.adapter_type ?? 'cheerio' })`

    2. In `lib/scraper/run.ts`:
       - After `const registry = await loadRegistry();` and before sharding, add:
         ```typescript
         const shardType = process.env.SHARD_TYPE ?? 'all';
         ```
       - Replace the existing shard filter line with:
         ```typescript
         const typeFiltered = shardType === 'all'
           ? registry
           : registry.filter((e) => e.adapterType === shardType);
         const shard = typeFiltered.filter((_, i) => i % shardTotal === shardIndex);
         ```
       - Update the summary log line to include shard type:
         ```typescript
         `[scraper] Summary -- shard ${shardIndex}/${shardTotal} (${shardType}): ` +
         ```

    3. Create `tests/scraper/run.test.ts`:
       - Mock `./registry` to export a `loadRegistry` that returns a fixed array of ResolvedEntry objects with different adapterTypes:
         - 3 entries with adapterType 'cheerio'
         - 1 entry with adapterType 'playwright'
         - 1 entry with adapterType 'paddleocr'
       - Mock `./runner` to export `runScraper` that returns a summary object and captures what entries it received
       - Mock `@next/env` to export `loadEnvConfig` as a no-op
       - Tests set process.env.SHARD_TYPE, SHARD_INDEX, SHARD_TOTAL before importing/calling main
       - NOTE: Since run.ts uses top-level await pattern via main(), the tests should extract the filtering logic into a testable function OR test by examining runScraper's received args
       - Alternative approach: Extract the filtering logic into an exported function `filterAndShard(registry, shardType, shardIndex, shardTotal)` in run.ts and test that directly. This avoids mocking complexity of the main() function.

    Recommended approach for testability:
    - Add to run.ts: `export function filterAndShard(registry: ResolvedEntry[], shardType: string, shardIndex: number, shardTotal: number): ResolvedEntry[]`
    - This function does the type filter + modulo shard
    - main() calls `filterAndShard(registry, shardType, shardIndex, shardTotal)` instead of inline logic
    - Tests import `filterAndShard` directly — no mocking needed

    Import the ResolvedEntry type: Since ResolvedEntry is not currently exported from registry.ts, either export it or define a compatible type in the test. Recommended: export `ResolvedEntry` from registry.ts.

    Test cases for filterAndShard:
    a. shardType='cheerio' returns only the 3 cheerio entries
    b. shardType='playwright' returns only the 1 playwright entry
    c. shardType='paddleocr' returns only the 1 paddleocr entry
    d. shardType='all' returns all 5 entries
    e. shardType='cheerio', shardIndex=0, shardTotal=2 returns entries 0 and 2 of the cheerio-filtered list (modulo after filter)
  </action>
  <acceptance_criteria>
    - grep "adapterType" lib/scraper/registry.ts returns matches
    - grep "SHARD_TYPE" lib/scraper/run.ts returns matches
    - grep "filterAndShard" lib/scraper/run.ts returns matches
    - grep -c "shard_type\|SHARD_TYPE\|filterAndShard" tests/scraper/run.test.ts returns >= 5
    - test -f tests/scraper/run.test.ts exits 0
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/scraper/run.test.ts tests/scraper/factory.test.ts</automated>
  </verify>
  <done>ResolvedEntry has adapterType field. run.ts filters by SHARD_TYPE before sharding via exported filterAndShard function. All 5 test cases pass. Existing factory tests still pass (regression).</done>
</task>

<task type="auto">
  <name>Task 2: Update workflow matrices to type-aware 24-job sharding</name>
  <files>.github/workflows/scrape-low.yml, .github/workflows/scrape-peak.yml</files>
  <read_first>
    - .github/workflows/scrape-low.yml (current 6-shard matrix)
    - .github/workflows/scrape-peak.yml (current 6-shard matrix)
    - .planning/phases/18-tohop-coverage-infrastructure-scale/18-RESEARCH.md (Pattern 3 and Pattern 4)
  </read_first>
  <action>
    For BOTH scrape-low.yml and scrape-peak.yml, make these identical changes:

    1. Replace `strategy.matrix.shard: [0, 1, 2, 3, 4, 5]` with `strategy.matrix.include:` containing 24 explicit objects:
       ```yaml
       strategy:
         matrix:
           include:
             # 20 cheerio shards
             - { shard: 0,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 1,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 2,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 3,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 4,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 5,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 6,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 7,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 8,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 9,  shard_type: cheerio,    shard_total: 20 }
             - { shard: 10, shard_type: cheerio,    shard_total: 20 }
             - { shard: 11, shard_type: cheerio,    shard_total: 20 }
             - { shard: 12, shard_type: cheerio,    shard_total: 20 }
             - { shard: 13, shard_type: cheerio,    shard_total: 20 }
             - { shard: 14, shard_type: cheerio,    shard_total: 20 }
             - { shard: 15, shard_type: cheerio,    shard_total: 20 }
             - { shard: 16, shard_type: cheerio,    shard_total: 20 }
             - { shard: 17, shard_type: cheerio,    shard_total: 20 }
             - { shard: 18, shard_type: cheerio,    shard_total: 20 }
             - { shard: 19, shard_type: cheerio,    shard_total: 20 }
             # 2 playwright shards
             - { shard: 0,  shard_type: playwright, shard_total: 2 }
             - { shard: 1,  shard_type: playwright, shard_total: 2 }
             # 2 paddleocr shards
             - { shard: 0,  shard_type: paddleocr,  shard_total: 2 }
             - { shard: 1,  shard_type: paddleocr,  shard_total: 2 }
         fail-fast: false
       ```

    2. Add `if:` conditions to Playwright setup steps:
       - `Cache Playwright browsers` step: add `if: matrix.shard_type == 'playwright'`
       - `Install Playwright browsers` step: change condition to `if: matrix.shard_type == 'playwright' && steps.playwright-cache.outputs.cache-hit != 'true'`
       - `Install Playwright OS dependencies` step: change condition to `if: matrix.shard_type == 'playwright' && steps.playwright-cache.outputs.cache-hit == 'true'`

    3. Add `if:` conditions to PaddleOCR setup steps:
       - `actions/setup-python@v5` step: add `if: matrix.shard_type == 'paddleocr'`
       - `Cache PaddleOCR models` step: add `if: matrix.shard_type == 'paddleocr'`
       - `Install PaddleOCR` step: add `if: matrix.shard_type == 'paddleocr'`
       - `Warm up PaddleOCR models` step: add `if: matrix.shard_type == 'paddleocr'`

    4. Update the run step env vars:
       - Change `SHARD_TOTAL: 6` to `SHARD_TOTAL: ${{ matrix.shard_total }}`
       - Add `SHARD_TYPE: ${{ matrix.shard_type }}`

    5. Update the run step name:
       - From: `Run scraper shard ${{ matrix.shard }}/6`
       - To: `Run scraper shard ${{ matrix.shard }}/${{ matrix.shard_total }} (${{ matrix.shard_type }})`

    6. Keep `PADDLE_PDX_MODEL_SOURCE` and `PADDLE_PDX_DISABLE_MODEL_SOURCE_CHECK` at job-level env (harmless for cheerio shards, avoids conditional env complexity).

    7. For scrape-peak.yml: Keep the existing `if:` on the job level for PEAK_SCHEDULE_ENABLED — do NOT remove it. Only add the step-level `if:` conditions within the steps.
  </action>
  <acceptance_criteria>
    - grep -c "shard_type" .github/workflows/scrape-low.yml returns >= 24
    - grep -c "shard_type" .github/workflows/scrape-peak.yml returns >= 24
    - grep "SHARD_TYPE" .github/workflows/scrape-low.yml returns match
    - grep "SHARD_TYPE" .github/workflows/scrape-peak.yml returns match
    - grep "matrix.shard_total" .github/workflows/scrape-low.yml returns match
    - grep "matrix.shard_total" .github/workflows/scrape-peak.yml returns match
    - grep -c "shard_type == 'playwright'" .github/workflows/scrape-low.yml returns >= 3
    - grep -c "shard_type == 'paddleocr'" .github/workflows/scrape-low.yml returns >= 4
  </acceptance_criteria>
  <verify>
    <automated>grep -c "shard_type" .github/workflows/scrape-low.yml && grep -c "shard_type" .github/workflows/scrape-peak.yml && grep "SHARD_TYPE" .github/workflows/scrape-low.yml && grep "SHARD_TYPE" .github/workflows/scrape-peak.yml</automated>
  </verify>
  <done>Both workflow files have 24-job type-aware matrix. Playwright setup only runs on playwright shards. PaddleOCR setup only runs on paddleocr shards. SHARD_TYPE and SHARD_TOTAL passed from matrix to run.ts via env vars.</done>
</task>

</tasks>

<verification>
- `npx vitest run tests/scraper/run.test.ts` — all SHARD_TYPE filtering tests pass
- `npx vitest run` — full test suite passes (no regressions)
- `grep -c "shard_type" .github/workflows/scrape-low.yml` returns >= 24
- `grep -c "shard_type" .github/workflows/scrape-peak.yml` returns >= 24
- `grep "adapterType" lib/scraper/registry.ts` returns match
- `grep "SHARD_TYPE" lib/scraper/run.ts` returns match
</verification>

<success_criteria>
- ResolvedEntry includes adapterType propagated from registry
- SHARD_TYPE env var filters registry before shard modulo distribution
- filterAndShard function is exported and tested with 5 test cases
- Both workflow files have 24 jobs (20 cheerio + 2 playwright + 2 paddleocr)
- Playwright/OCR setup steps gated by shard_type condition
- Cheerio shards skip Playwright install and PaddleOCR warmup
- Full test suite passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-tohop-coverage-infrastructure-scale/18-02-SUMMARY.md`
</output>
